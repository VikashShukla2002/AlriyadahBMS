@if (!hijriTableList.IsExport()) {
<div>
@if (!hijriTableList.IsGridAdd && !(hijriTableList.IsGridEdit && hijriTableList.ModalGridEdit) && !hijriTableList.IsMultiEdit) {
@Html.Raw(hijriTableList.Pager.Render())
}
<div class="ew-list-other-options">
@hijriTableList.OtherOptions.RenderBody()
</div>
</div>
}
@if (hijriTableList.ModalGridAdd && hijriTableList.IsGridAdd || hijriTableList.ModalGridEdit && hijriTableList.IsGridEdit || hijriTableList.ModalMultiEdit && hijriTableList.IsMultiEdit) {
<script>
var currentTable = @Html.Raw(ConvertToJson(hijriTableList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { Hijri_Table: currentTable } });
var currentPageID = ew.PAGE_ID = "list";
var currentForm;
var @(hijriTableList.FormName);
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("@(hijriTableList.FormName)")
        .setPageId("list")
        .setSubmitWithFetch(@(hijriTableList.UseAjaxActions ? "true" : "false"))
        .setFormKeyCountName("@(hijriTableList.FormKeyCountName)")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
ew.PREVIEW_SELECTOR = ".ew-preview-btn";
ew.PREVIEW_MODAL_CLASS = "modal modal-fullscreen-sm-down";
ew.PREVIEW_ROW = true;
ew.PREVIEW_SINGLE_ROW = false;
ew.PREVIEW || ew.ready("head", ew.PATH_BASE + "js/preview.min.js?v=20.5.0", "preview");
</script>
}
<form name="@(hijriTableList.FormName)" id="@(hijriTableList.FormName)" class="ew-form ew-list-form ew-multi-column-form" action="@AppPath(hijriTableList.PageName)" method="post" novalidate autocomplete="off">
@if (CurrentPage.CheckToken) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
@if (hijriTableList.IsModal) {
<input type="hidden" name="modal" value="1">
}
<div class="@(hijriTableList.GetMultiColumnRowClass())">
@if (hijriTableList.TotalRecords > 0 || hijriTableList.IsGridEdit || hijriTableList.IsMultiEdit) {
    await hijriTableList.SetupGrid();
    bool eof = hijriTableList.Recordset == null; // DN
while (hijriTableList.RecordCount < hijriTableList.StopRecord || SameString(hijriTableList.RowIndex, "$rowindex$")) { // Begin While
    if (!eof && !SameString(hijriTableList.RowIndex, "$rowindex$") &&
        (!hijriTableList.IsGridAdd || hijriTableList.CurrentMode == "copy") &&
        (!((hijriTableList.IsCopy || hijriTableList.IsAdd) && hijriTableList.RowIndex is int rowIndex && rowIndex == 0))) // DN
        eof = !await hijriTableList.Recordset.ReadAsync();
    hijriTableList.RecordCount++;
    if (hijriTableList.RecordCount >= hijriTableList.StartRecord) {
        await hijriTableList.SetupRow();
<div class="@(hijriTableList.GetMultiColumnColClass())" @Html.Raw(hijriTableList.RowAttributes)>
<div class="@(hijriTableList.MultiColumnCardClass)">
    @if (hijriTableList.MultiColumnListOptionsPosition.StartsWith("top", StringComparison.InvariantCultureIgnoreCase)) {
    <div class="card-header">
        <div class="ew-multi-column-list-option ew-@(hijriTableList.MultiColumnListOptionsPosition)">
@{
    // Render list options (body, bottom)
    hijriTableList.ListOptions.Tag = "div";
    hijriTableList.ListOptions.RenderBody(hijriTableList.MultiColumnListOptionsPosition, hijriTableList.RowCount);
}
        </div><!-- /.ew-multi-column-list-option -->
    </div>
    }
    <div class="card-body">
    @if (hijriTableList.ID.Visible) { // hijriTableList.ID.Visible (begin)
        if (hijriTableList.RowType == RowType.View) { // View record
        <div class="row Hijri_Table_ID">
            <div class="col col-sm-4 Hijri_Table_ID">@Html.Raw(hijriTableList.RenderFieldHeader(hijriTableList.ID))</div>
            <div class="col col-sm-8"><div @Html.Raw(hijriTableList.ID.CellAttributes)>
<span @Html.Raw(hijriTableList.ID.ViewAttributes)>
@Html.Raw(hijriTableList.ID.GetViewValue())</span>
</div></div>
        </div>
        } else { // Add/edit record
        <div class="row Hijri_Table_ID">
            <label class="@(hijriTableList.LeftColumnClass)">@Html.Raw(hijriTableList.ID.Caption)</label>
            <div class="@(hijriTableList.RightColumnClass)"><div @Html.Raw(hijriTableList.ID.CellAttributes)>
<span @Html.Raw(hijriTableList.ID.ViewAttributes)>
@Html.Raw(hijriTableList.ID.GetViewValue())</span>
</div></div>
        </div>
        }
    }
    @if (hijriTableList.Hijri_Year.Visible) { // hijriTableList.Hijri_Year.Visible (begin)
        if (hijriTableList.RowType == RowType.View) { // View record
        <div class="row Hijri_Table_Hijri_Year">
            <div class="col col-sm-4 Hijri_Table_Hijri_Year">@Html.Raw(hijriTableList.RenderFieldHeader(hijriTableList.Hijri_Year))</div>
            <div class="col col-sm-8"><div @Html.Raw(hijriTableList.Hijri_Year.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_Year.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_Year.GetViewValue())</span>
</div></div>
        </div>
        } else { // Add/edit record
        <div class="row Hijri_Table_Hijri_Year">
            <label class="@(hijriTableList.LeftColumnClass)">@Html.Raw(hijriTableList.Hijri_Year.Caption)</label>
            <div class="@(hijriTableList.RightColumnClass)"><div @Html.Raw(hijriTableList.Hijri_Year.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_Year.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_Year.GetViewValue())</span>
</div></div>
        </div>
        }
    }
    @if (hijriTableList.Hijri_Month.Visible) { // hijriTableList.Hijri_Month.Visible (begin)
        if (hijriTableList.RowType == RowType.View) { // View record
        <div class="row Hijri_Table_Hijri_Month">
            <div class="col col-sm-4 Hijri_Table_Hijri_Month">@Html.Raw(hijriTableList.RenderFieldHeader(hijriTableList.Hijri_Month))</div>
            <div class="col col-sm-8"><div @Html.Raw(hijriTableList.Hijri_Month.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_Month.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_Month.GetViewValue())</span>
</div></div>
        </div>
        } else { // Add/edit record
        <div class="row Hijri_Table_Hijri_Month">
            <label class="@(hijriTableList.LeftColumnClass)">@Html.Raw(hijriTableList.Hijri_Month.Caption)</label>
            <div class="@(hijriTableList.RightColumnClass)"><div @Html.Raw(hijriTableList.Hijri_Month.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_Month.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_Month.GetViewValue())</span>
</div></div>
        </div>
        }
    }
    @if (hijriTableList.Hijri_Monthname.Visible) { // hijriTableList.Hijri_Monthname.Visible (begin)
        if (hijriTableList.RowType == RowType.View) { // View record
        <div class="row Hijri_Table_Hijri_Monthname">
            <div class="col col-sm-4 Hijri_Table_Hijri_Monthname">@Html.Raw(hijriTableList.RenderFieldHeader(hijriTableList.Hijri_Monthname))</div>
            <div class="col col-sm-8"><div @Html.Raw(hijriTableList.Hijri_Monthname.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_Monthname.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_Monthname.GetViewValue())</span>
</div></div>
        </div>
        } else { // Add/edit record
        <div class="row Hijri_Table_Hijri_Monthname">
            <label class="@(hijriTableList.LeftColumnClass)">@Html.Raw(hijriTableList.Hijri_Monthname.Caption)</label>
            <div class="@(hijriTableList.RightColumnClass)"><div @Html.Raw(hijriTableList.Hijri_Monthname.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_Monthname.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_Monthname.GetViewValue())</span>
</div></div>
        </div>
        }
    }
    @if (hijriTableList.Hijri_Day.Visible) { // hijriTableList.Hijri_Day.Visible (begin)
        if (hijriTableList.RowType == RowType.View) { // View record
        <div class="row Hijri_Table_Hijri_Day">
            <div class="col col-sm-4 Hijri_Table_Hijri_Day">@Html.Raw(hijriTableList.RenderFieldHeader(hijriTableList.Hijri_Day))</div>
            <div class="col col-sm-8"><div @Html.Raw(hijriTableList.Hijri_Day.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_Day.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_Day.GetViewValue())</span>
</div></div>
        </div>
        } else { // Add/edit record
        <div class="row Hijri_Table_Hijri_Day">
            <label class="@(hijriTableList.LeftColumnClass)">@Html.Raw(hijriTableList.Hijri_Day.Caption)</label>
            <div class="@(hijriTableList.RightColumnClass)"><div @Html.Raw(hijriTableList.Hijri_Day.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_Day.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_Day.GetViewValue())</span>
</div></div>
        </div>
        }
    }
    @if (hijriTableList.Hijri_mon_ar.Visible) { // hijriTableList.Hijri_mon_ar.Visible (begin)
        if (hijriTableList.RowType == RowType.View) { // View record
        <div class="row Hijri_Table_Hijri_mon_ar">
            <div class="col col-sm-4 Hijri_Table_Hijri_mon_ar">@Html.Raw(hijriTableList.RenderFieldHeader(hijriTableList.Hijri_mon_ar))</div>
            <div class="col col-sm-8"><div @Html.Raw(hijriTableList.Hijri_mon_ar.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_mon_ar.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_mon_ar.GetViewValue())</span>
</div></div>
        </div>
        } else { // Add/edit record
        <div class="row Hijri_Table_Hijri_mon_ar">
            <label class="@(hijriTableList.LeftColumnClass)">@Html.Raw(hijriTableList.Hijri_mon_ar.Caption)</label>
            <div class="@(hijriTableList.RightColumnClass)"><div @Html.Raw(hijriTableList.Hijri_mon_ar.CellAttributes)>
<span @Html.Raw(hijriTableList.Hijri_mon_ar.ViewAttributes)>
@Html.Raw(hijriTableList.Hijri_mon_ar.GetViewValue())</span>
</div></div>
        </div>
        }
    }
    </div><!-- /.card-body -->
@if (!hijriTableList.IsExport()) {
    if (hijriTableList.MultiColumnListOptionsPosition.StartsWith("bottom", StringComparison.InvariantCultureIgnoreCase)) {
    <div class="card-footer">
        <div class="ew-multi-column-list-option ew-@(hijriTableList.MultiColumnListOptionsPosition)">
@{
    // Render list options (body, bottom)
    hijriTableList.ListOptions.Tag = "div";
}
@hijriTableList.ListOptions.RenderBody(hijriTableList.MultiColumnListOptionsPosition, hijriTableList.RowCount)
        </div><!-- /.ew-multi-column-list-option -->
    </div><!-- /.card-footer -->
    }
}
</div><!-- /.card -->
</div><!-- /.col-* -->
    } @* End if *@

    // Reset for template row
    if (SameString(hijriTableList.RowIndex, "$rowindex$"))
        hijriTableList.RowIndex = 0;

    // Reset inline add/copy row
    if ((hijriTableList.IsCopy || hijriTableList.IsAdd) && hijriTableList.RowIndex is int rowIndex2 && rowIndex2 == 0)
        hijriTableList.RowIndex = 1;
} @* End While *@
} @* hijriTableList.TotalRecords > 0 (end) *@
</div><!-- /.ew-multi-column-row -->
@if (Empty(hijriTableList.CurrentAction) && !hijriTableList.UseAjaxActions) {
<input type="hidden" name="action" id="action" value="">
}
</form><!-- /.ew-list-form -->
@{ hijriTableList.CloseRecordset(); }
