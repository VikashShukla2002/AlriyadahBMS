@{
    // View
    View = this;

    // Check action result
    if (tblNationalityList.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!tblNationalityList.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
@if (!tblNationalityList.IsExport()) {
<script>
var currentTable = @Html.Raw(ConvertToJson(tblNationalityList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { tblNationality: currentTable } });
var currentPageID = ew.PAGE_ID = "list";
var currentForm;
var @(tblNationalityList.FormName);
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("@(tblNationalityList.FormName)")
        .setPageId("list")
        .setSubmitWithFetch(@(tblNationalityList.UseAjaxActions ? "true" : "false"))
        .setFormKeyCountName("@(tblNationalityList.FormKeyCountName)")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
ew.PREVIEW_SELECTOR = ".ew-preview-btn";
ew.PREVIEW_MODAL_CLASS = "modal modal-fullscreen-sm-down";
ew.PREVIEW_ROW = true;
ew.PREVIEW_SINGLE_ROW = false;
ew.PREVIEW || ew.ready("head", ew.PATH_BASE + "js/preview.min.js?v=20.5.0", "preview");
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
}
@if (!tblNationalityList.IsExport()) {
<div class="btn-toolbar ew-toolbar">
@if (tblNationalityList.TotalRecords > 0 && tblNationalityList.ExportOptions.Visible) {
    tblNationalityList.ExportOptions.Render("body");
}
@if (tblNationalityList.ImportOptions.Visible) {
    tblNationalityList.ImportOptions.Render("body");
}
@if (tblNationalityList.SearchOptions.Visible) {
    tblNationalityList.SearchOptions.Render("body");
}
@if (tblNationalityList.FilterOptions.Visible) {
    tblNationalityList.FilterOptions.Render("body");
}
</div>
}
@if (!tblNationalityList.IsModal) {
<form name="ftblNationalitysrch" id="ftblNationalitysrch" class="ew-form ew-ext-search-form" action="@AppPath(CurrentPageName())" novalidate autocomplete="off">
<div id="ftblNationalitysrch_search_panel" class="mb-2 mb-sm-0 @(tblNationalityList.SearchPanelClass)"><!-- .ew-search-panel -->
<script>
var currentTable = @Html.Raw(ConvertToJson(tblNationalityList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { tblNationality: currentTable } });
var currentForm;
var ftblNationalitysrch, currentSearchForm, currentAdvancedSearchForm;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery,
        fields = currentTable.fields;

    // Form object for search
    let form = new ew.FormBuilder()
        .setId("ftblNationalitysrch")
        .setPageId("list")
@if (tblNationalityList.UseAjaxActions) {
        <text>.setSubmitWithFetch(true)</text>
}

        // Dynamic selection lists
        .setLists({
        })

        // Filters
        .setFilterList(@Html.Raw(await tblNationalityList.GetFilterList()))
        .build();
    window[form.id] = form;
    currentSearchForm = form;
    loadjs.done(form.id);
});
</script>
<input type="hidden" name="cmd" value="search">
@if (Security.CanSearch) {
if (!tblNationalityList.IsExport() && Empty(tblNationalityList.CurrentAction) && tblNationalityList.HasSearchFields()) { @* Show search form (begin) *@
<div class="ew-extended-search container-fluid ps-2">
<div class="row mb-0">
    <div class="col-sm-auto px-0 pe-sm-2">
        <div class="ew-basic-search input-group">
            <input type="search" name="@(Config.TableBasicSearch)" id="@(Config.TableBasicSearch)" class="form-control ew-basic-search-keyword" value="@Html.Raw(HtmlEncode(tblNationalityList.BasicSearch.SessionKeyword))" placeholder="@(HtmlEncode(Language.Phrase("Search")))" aria-label="@(HtmlEncode(Language.Phrase("Search")))">
            <input type="hidden" name="@(Config.TableBasicSearchType)" id="@(Config.TableBasicSearchType)" class="ew-basic-search-type" value="@(HtmlEncode(tblNationalityList.BasicSearch.SessionType))">
            <button type="button" data-bs-toggle="dropdown" class="btn btn-outline-secondary dropdown-toggle dropdown-toggle-split" aria-haspopup="true" aria-expanded="false">
                <span id="searchtype">@Html.Raw(tblNationalityList.BasicSearch.TypeNameShort)</span>
            </button>
            <div class="dropdown-menu dropdown-menu-end">
                <button type="button" class="dropdown-item@(tblNationalityList.BasicSearch.SessionType == "" ? " active" : "")" form="ftblNationalitysrch" data-ew-action="search-type">@Html.Raw(Language.Phrase("QuickSearchAuto"))</button>
                <button type="button" class="dropdown-item@(tblNationalityList.BasicSearch.SessionType == "=" ? " active" : "")" form="ftblNationalitysrch" data-ew-action="search-type" data-search-type="=">@Html.Raw(Language.Phrase("QuickSearchExact"))</button>
                <button type="button" class="dropdown-item@(tblNationalityList.BasicSearch.SessionType == "AND" ? " active" : "")" form="ftblNationalitysrch" data-ew-action="search-type" data-search-type="AND">@Html.Raw(Language.Phrase("QuickSearchAll"))</button>
                <button type="button" class="dropdown-item@(tblNationalityList.BasicSearch.SessionType == "OR" ? " active" : "")" form="ftblNationalitysrch" data-ew-action="search-type" data-search-type="OR">@Html.Raw(Language.Phrase("QuickSearchAny"))</button>
            </div>
        </div>
    </div>
    <div class="col-sm-auto mb-3">
        <button class="btn btn-primary" name="btn-submit" id="btn-submit" type="submit">@Html.Raw(Language.Phrase("SearchBtn"))</button>
    </div>
</div>
</div><!-- /.ew-extended-search -->
} @* Show search form (end) *@
}
</div><!-- /.ew-search-panel -->
</form>
}
@tblNationalityList.ShowPageHeader()
@tblNationalityList.ShowMessages()
<main class="list @(tblNationalityList.TotalRecords == 0 && !tblNationalityList.IsAdd ? " ew-no-record" : "")">
<div id="ew-list">
@if (tblNationalityList.TotalRecords > 0 || !Empty(tblNationalityList.CurrentAction)) {
<div class="ew-multi-column-grid p-0 overlay-wrapper">
@{ tblNationalityList.LayoutOptions.Render("body"); }
@await Html.PartialAsync("TblNationality" + TitleCaseInvariant(tblNationalityList.MultiColumnLayout))
</div><!-- /.ew-multi-column-grid -->
} else { // Show other options
<div class="ew-list-other-options">
@tblNationalityList.OtherOptions.ForEach(opt => opt.ButtonClass = "").RenderBody()
</div>
}
</div>
</main>
@tblNationalityList.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
@if (!tblNationalityList.IsExport()) {
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("tblNationality");
});
</script>
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
}
