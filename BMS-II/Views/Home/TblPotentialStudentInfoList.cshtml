@{
    // View
    View = this;

    // Check action result
    if (tblPotentialStudentInfoList.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!tblPotentialStudentInfoList.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
@if (!tblPotentialStudentInfoList.IsExport()) {
<script>
var currentTable = @Html.Raw(ConvertToJson(tblPotentialStudentInfoList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { tblPotentialStudentInfo: currentTable } });
var currentPageID = ew.PAGE_ID = "list";
var currentForm;
var @(tblPotentialStudentInfoList.FormName);
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("@(tblPotentialStudentInfoList.FormName)")
        .setPageId("list")
        .setSubmitWithFetch(@(tblPotentialStudentInfoList.UseAjaxActions ? "true" : "false"))
        .setFormKeyCountName("@(tblPotentialStudentInfoList.FormKeyCountName)")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
ew.PREVIEW_SELECTOR = ".ew-preview-btn";
ew.PREVIEW_MODAL_CLASS = "modal modal-fullscreen-sm-down";
ew.PREVIEW_ROW = true;
ew.PREVIEW_SINGLE_ROW = false;
ew.PREVIEW || ew.ready("head", ew.PATH_BASE + "js/preview.min.js?v=20.5.0", "preview");
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
}
@if (!tblPotentialStudentInfoList.IsExport()) {
<div class="btn-toolbar ew-toolbar">
@if (tblPotentialStudentInfoList.TotalRecords > 0 && tblPotentialStudentInfoList.ExportOptions.Visible) {
    tblPotentialStudentInfoList.ExportOptions.Render("body");
}
@if (tblPotentialStudentInfoList.ImportOptions.Visible) {
    tblPotentialStudentInfoList.ImportOptions.Render("body");
}
@if (tblPotentialStudentInfoList.SearchOptions.Visible) {
    tblPotentialStudentInfoList.SearchOptions.Render("body");
}
@if (tblPotentialStudentInfoList.FilterOptions.Visible) {
    tblPotentialStudentInfoList.FilterOptions.Render("body");
}
</div>
}
@if (tblPotentialStudentInfoList.ShowCurrentFilter) {
    tblPotentialStudentInfoList.ShowFilterList();
}
@if (!tblPotentialStudentInfoList.IsModal) {
<form name="ftblPotentialStudentInfosrch" id="ftblPotentialStudentInfosrch" class="ew-form ew-ext-search-form" action="@AppPath(CurrentPageName())" novalidate autocomplete="off">
<div id="ftblPotentialStudentInfosrch_search_panel" class="mb-2 mb-sm-0 @(tblPotentialStudentInfoList.SearchPanelClass)"><!-- .ew-search-panel -->
<script>
var currentTable = @Html.Raw(ConvertToJson(tblPotentialStudentInfoList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { tblPotentialStudentInfo: currentTable } });
var currentForm;
var ftblPotentialStudentInfosrch, currentSearchForm, currentAdvancedSearchForm;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery,
        fields = currentTable.fields;

    // Form object for search
    let form = new ew.FormBuilder()
        .setId("ftblPotentialStudentInfosrch")
        .setPageId("list")
@if (tblPotentialStudentInfoList.UseAjaxActions) {
        <text>.setSubmitWithFetch(true)</text>
}

        // Add fields
        .setFields([

            ["NationalID", [ew.Validators.integer], fields.NationalID.isInvalid],
            ["str_First_Name", [], fields.str_First_Name.isInvalid],
            ["str_Last_Name", [], fields.str_Last_Name.isInvalid],
            ["str_Cell_Phone", [], fields.str_Cell_Phone.isInvalid],
            ["Required_Program", [], fields.Required_Program.isInvalid],
            ["Price", [], fields.Price.isInvalid],
            ["Payment_Online", [], fields.Payment_Online.isInvalid]
        ])
        // Validate form
        .setValidate(
            async function () {
                if (!this.validateRequired)
                    return true; // Ignore validation
                let fobj = this.getForm();

                // Validate fields
                if (!this.validateFields())
                    return false;

                // Call Form_CustomValidate event
                if (!(await this.customValidate?.(fobj) ?? true)) {
                    this.focus();
                    return false;
                }
                return true;
            }
        )

        // Form_CustomValidate
        .setCustomValidate(
            function(fobj) { // DO NOT CHANGE THIS LINE!
                    // Your custom validation code here, return false if invalid.
                    return true;
                }
        )

        // Use JavaScript validation or not
        .setValidateRequired(ew.CLIENT_VALIDATE)

        // Dynamic selection lists
        .setLists({
        })

        // Filters
        .setFilterList(@Html.Raw(await tblPotentialStudentInfoList.GetFilterList()))
        .build();
    window[form.id] = form;
    currentSearchForm = form;
    loadjs.done(form.id);
});
</script>
<input type="hidden" name="cmd" value="search">
@if (Security.CanSearch) {
if (!tblPotentialStudentInfoList.IsExport() && Empty(tblPotentialStudentInfoList.CurrentAction) && tblPotentialStudentInfoList.HasSearchFields()) { @* Show search form (begin) *@
<div class="ew-extended-search container-fluid ps-2">
<div class="row mb-0@(tblPotentialStudentInfoList.SearchFieldsPerRow > 0 ? " row-cols-sm-" + tblPotentialStudentInfoList.SearchFieldsPerRow : "")">
@{
    // Render search row
    tblPotentialStudentInfoList.RowType = RowType.Search;
    tblPotentialStudentInfoList.ResetAttributes();
    await tblPotentialStudentInfoList.RenderRow();
}
@if (tblPotentialStudentInfoList.NationalID.Visible) { // tblPotentialStudentInfoList.NationalID.Visible (begin)
    if (!tblPotentialStudentInfoList.NationalID.UseFilter) {
        tblPotentialStudentInfoList.SearchColumnCount++;
    }
    <div id="xs_NationalID" class="col-sm-auto d-sm-flex align-items-start mb-3 px-0 pe-sm-2@(tblPotentialStudentInfoList.NationalID.UseFilter ? " ew-filter-field" : "")">
        <div class="d-flex my-1 my-sm-0">
            <label for="x_NationalID" class="ew-search-caption ew-label">@Html.Raw(tblPotentialStudentInfoList.NationalID.Caption)</label>
            <div class="ew-search-operator">
@Html.Raw(Language.Phrase("="))
<input type="hidden" name="z_NationalID" id="z_NationalID" value="=">
</div>
        </div>
        <div id="el_tblPotentialStudentInfo_NationalID" class="ew-search-field">
<input type="@(tblPotentialStudentInfoList.NationalID.InputTextType)" name="x_NationalID" id="x_NationalID" data-table="tblPotentialStudentInfo" data-field="x_NationalID" value="@Html.Raw(tblPotentialStudentInfoList.NationalID.EditValue)" size="30" placeholder="@Html.Raw(HtmlEncode(tblPotentialStudentInfoList.NationalID.PlaceHolder))" data-format-pattern="@(tblPotentialStudentInfoList.NationalID.FormatPattern)"@Html.Raw(tblPotentialStudentInfoList.NationalID.EditAttributes)>
<div class="invalid-feedback">@Html.Raw(tblPotentialStudentInfoList.NationalID.GetErrorMessage(false))</div>
</div>
        <div class="d-flex my-1 my-sm-0">
        </div><!-- /.ew-search-field -->
    </div><!-- /.col-sm-auto -->
} @* tblPotentialStudentInfoList.NationalID.Visible (end) *@
@if (tblPotentialStudentInfoList.str_Cell_Phone.Visible) { // tblPotentialStudentInfoList.str_Cell_Phone.Visible (begin)
    if (!tblPotentialStudentInfoList.str_Cell_Phone.UseFilter) {
        tblPotentialStudentInfoList.SearchColumnCount++;
    }
    <div id="xs_str_Cell_Phone" class="col-sm-auto d-sm-flex align-items-start mb-3 px-0 pe-sm-2@(tblPotentialStudentInfoList.str_Cell_Phone.UseFilter ? " ew-filter-field" : "")">
        <div class="d-flex my-1 my-sm-0">
            <label for="x_str_Cell_Phone" class="ew-search-caption ew-label">@Html.Raw(tblPotentialStudentInfoList.str_Cell_Phone.Caption)</label>
            <div class="ew-search-operator">
@Html.Raw(Language.Phrase("LIKE"))
<input type="hidden" name="z_str_Cell_Phone" id="z_str_Cell_Phone" value="LIKE">
</div>
        </div>
        <div id="el_tblPotentialStudentInfo_str_Cell_Phone" class="ew-search-field">
<input type="@(tblPotentialStudentInfoList.str_Cell_Phone.InputTextType)" name="x_str_Cell_Phone" id="x_str_Cell_Phone" data-table="tblPotentialStudentInfo" data-field="x_str_Cell_Phone" value="@Html.Raw(tblPotentialStudentInfoList.str_Cell_Phone.EditValue)" size="30" maxlength="50" placeholder="@Html.Raw(HtmlEncode(tblPotentialStudentInfoList.str_Cell_Phone.PlaceHolder))" data-format-pattern="@(tblPotentialStudentInfoList.str_Cell_Phone.FormatPattern)"@Html.Raw(tblPotentialStudentInfoList.str_Cell_Phone.EditAttributes)>
<div class="invalid-feedback">@Html.Raw(tblPotentialStudentInfoList.str_Cell_Phone.GetErrorMessage(false))</div>
</div>
        <div class="d-flex my-1 my-sm-0">
        </div><!-- /.ew-search-field -->
    </div><!-- /.col-sm-auto -->
} @* tblPotentialStudentInfoList.str_Cell_Phone.Visible (end) *@
</div><!-- /.row -->
<div class="row mb-0">
    <div class="col-sm-auto px-0 pe-sm-2">
        <div class="ew-basic-search input-group">
            <input type="search" name="@(Config.TableBasicSearch)" id="@(Config.TableBasicSearch)" class="form-control ew-basic-search-keyword" value="@Html.Raw(HtmlEncode(tblPotentialStudentInfoList.BasicSearch.SessionKeyword))" placeholder="@(HtmlEncode(Language.Phrase("Search")))" aria-label="@(HtmlEncode(Language.Phrase("Search")))">
            <input type="hidden" name="@(Config.TableBasicSearchType)" id="@(Config.TableBasicSearchType)" class="ew-basic-search-type" value="@(HtmlEncode(tblPotentialStudentInfoList.BasicSearch.SessionType))">
            <button type="button" data-bs-toggle="dropdown" class="btn btn-outline-secondary dropdown-toggle dropdown-toggle-split" aria-haspopup="true" aria-expanded="false">
                <span id="searchtype">@Html.Raw(tblPotentialStudentInfoList.BasicSearch.TypeNameShort)</span>
            </button>
            <div class="dropdown-menu dropdown-menu-end">
                <button type="button" class="dropdown-item@(tblPotentialStudentInfoList.BasicSearch.SessionType == "" ? " active" : "")" form="ftblPotentialStudentInfosrch" data-ew-action="search-type">@Html.Raw(Language.Phrase("QuickSearchAuto"))</button>
                <button type="button" class="dropdown-item@(tblPotentialStudentInfoList.BasicSearch.SessionType == "=" ? " active" : "")" form="ftblPotentialStudentInfosrch" data-ew-action="search-type" data-search-type="=">@Html.Raw(Language.Phrase("QuickSearchExact"))</button>
                <button type="button" class="dropdown-item@(tblPotentialStudentInfoList.BasicSearch.SessionType == "AND" ? " active" : "")" form="ftblPotentialStudentInfosrch" data-ew-action="search-type" data-search-type="AND">@Html.Raw(Language.Phrase("QuickSearchAll"))</button>
                <button type="button" class="dropdown-item@(tblPotentialStudentInfoList.BasicSearch.SessionType == "OR" ? " active" : "")" form="ftblPotentialStudentInfosrch" data-ew-action="search-type" data-search-type="OR">@Html.Raw(Language.Phrase("QuickSearchAny"))</button>
            </div>
        </div>
    </div>
    <div class="col-sm-auto mb-3">
        <button class="btn btn-primary" name="btn-submit" id="btn-submit" type="submit">@Html.Raw(Language.Phrase("SearchBtn"))</button>
    </div>
</div>
</div><!-- /.ew-extended-search -->
} @* Show search form (end) *@
}
</div><!-- /.ew-search-panel -->
</form>
}
@tblPotentialStudentInfoList.ShowPageHeader()
@tblPotentialStudentInfoList.ShowMessages()
<main class="list @(tblPotentialStudentInfoList.TotalRecords == 0 && !tblPotentialStudentInfoList.IsAdd ? " ew-no-record" : "")">
<div id="ew-list">
@if (tblPotentialStudentInfoList.TotalRecords > 0 || !Empty(tblPotentialStudentInfoList.CurrentAction)) {
<div class="ew-multi-column-grid p-0 overlay-wrapper">
@{ tblPotentialStudentInfoList.LayoutOptions.Render("body"); }
@await Html.PartialAsync("TblPotentialStudentInfo" + TitleCaseInvariant(tblPotentialStudentInfoList.MultiColumnLayout))
</div><!-- /.ew-multi-column-grid -->
} else { // Show other options
<div class="ew-list-other-options">
@tblPotentialStudentInfoList.OtherOptions.ForEach(opt => opt.ButtonClass = "").RenderBody()
</div>
}
</div>
</main>
@tblPotentialStudentInfoList.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
@if (!tblPotentialStudentInfoList.IsExport()) {
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("tblPotentialStudentInfo");
});
</script>
<script>
loadjs.ready("load", function() {
    // Startup script
    // Write your table-specific startup script here
    // document.write("page loaded");
    // $('.card', '#r1_tblPotentialStudentInfo').addClass('border-0')
    $('.card', $('[id*="tblPotentialStudentInfo"]')).addClass('border-0')
});
</script>
}
