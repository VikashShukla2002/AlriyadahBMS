@{
    // View
    View = this;

    // Check action result
    if (hijriTableList.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!hijriTableList.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
@if (!hijriTableList.IsExport()) {
<script>
var currentTable = @Html.Raw(ConvertToJson(hijriTableList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { Hijri_Table: currentTable } });
var currentPageID = ew.PAGE_ID = "list";
var currentForm;
var @(hijriTableList.FormName);
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("@(hijriTableList.FormName)")
        .setPageId("list")
        .setSubmitWithFetch(@(hijriTableList.UseAjaxActions ? "true" : "false"))
        .setFormKeyCountName("@(hijriTableList.FormKeyCountName)")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
ew.PREVIEW_SELECTOR = ".ew-preview-btn";
ew.PREVIEW_MODAL_CLASS = "modal modal-fullscreen-sm-down";
ew.PREVIEW_ROW = true;
ew.PREVIEW_SINGLE_ROW = false;
ew.PREVIEW || ew.ready("head", ew.PATH_BASE + "js/preview.min.js?v=20.5.0", "preview");
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
}
@if (!hijriTableList.IsExport()) {
<div class="btn-toolbar ew-toolbar">
@if (hijriTableList.TotalRecords > 0 && hijriTableList.ExportOptions.Visible) {
    hijriTableList.ExportOptions.Render("body");
}
@if (hijriTableList.ImportOptions.Visible) {
    hijriTableList.ImportOptions.Render("body");
}
@if (hijriTableList.SearchOptions.Visible) {
    hijriTableList.SearchOptions.Render("body");
}
@if (hijriTableList.FilterOptions.Visible) {
    hijriTableList.FilterOptions.Render("body");
}
</div>
}
@if (!hijriTableList.IsModal) {
<form name="fHijri_Tablesrch" id="fHijri_Tablesrch" class="ew-form ew-ext-search-form" action="@AppPath(CurrentPageName())" novalidate autocomplete="off">
<div id="fHijri_Tablesrch_search_panel" class="mb-2 mb-sm-0 @(hijriTableList.SearchPanelClass)"><!-- .ew-search-panel -->
<script>
var currentTable = @Html.Raw(ConvertToJson(hijriTableList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { Hijri_Table: currentTable } });
var currentForm;
var fHijri_Tablesrch, currentSearchForm, currentAdvancedSearchForm;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery,
        fields = currentTable.fields;

    // Form object for search
    let form = new ew.FormBuilder()
        .setId("fHijri_Tablesrch")
        .setPageId("list")
@if (hijriTableList.UseAjaxActions) {
        <text>.setSubmitWithFetch(true)</text>
}

        // Dynamic selection lists
        .setLists({
        })

        // Filters
        .setFilterList(@Html.Raw(await hijriTableList.GetFilterList()))
        .build();
    window[form.id] = form;
    currentSearchForm = form;
    loadjs.done(form.id);
});
</script>
<input type="hidden" name="cmd" value="search">
@if (Security.CanSearch) {
if (!hijriTableList.IsExport() && Empty(hijriTableList.CurrentAction) && hijriTableList.HasSearchFields()) { @* Show search form (begin) *@
<div class="ew-extended-search container-fluid ps-2">
<div class="row mb-0">
    <div class="col-sm-auto px-0 pe-sm-2">
        <div class="ew-basic-search input-group">
            <input type="search" name="@(Config.TableBasicSearch)" id="@(Config.TableBasicSearch)" class="form-control ew-basic-search-keyword" value="@Html.Raw(HtmlEncode(hijriTableList.BasicSearch.SessionKeyword))" placeholder="@(HtmlEncode(Language.Phrase("Search")))" aria-label="@(HtmlEncode(Language.Phrase("Search")))">
            <input type="hidden" name="@(Config.TableBasicSearchType)" id="@(Config.TableBasicSearchType)" class="ew-basic-search-type" value="@(HtmlEncode(hijriTableList.BasicSearch.SessionType))">
            <button type="button" data-bs-toggle="dropdown" class="btn btn-outline-secondary dropdown-toggle dropdown-toggle-split" aria-haspopup="true" aria-expanded="false">
                <span id="searchtype">@Html.Raw(hijriTableList.BasicSearch.TypeNameShort)</span>
            </button>
            <div class="dropdown-menu dropdown-menu-end">
                <button type="button" class="dropdown-item@(hijriTableList.BasicSearch.SessionType == "" ? " active" : "")" form="fHijri_Tablesrch" data-ew-action="search-type">@Html.Raw(Language.Phrase("QuickSearchAuto"))</button>
                <button type="button" class="dropdown-item@(hijriTableList.BasicSearch.SessionType == "=" ? " active" : "")" form="fHijri_Tablesrch" data-ew-action="search-type" data-search-type="=">@Html.Raw(Language.Phrase("QuickSearchExact"))</button>
                <button type="button" class="dropdown-item@(hijriTableList.BasicSearch.SessionType == "AND" ? " active" : "")" form="fHijri_Tablesrch" data-ew-action="search-type" data-search-type="AND">@Html.Raw(Language.Phrase("QuickSearchAll"))</button>
                <button type="button" class="dropdown-item@(hijriTableList.BasicSearch.SessionType == "OR" ? " active" : "")" form="fHijri_Tablesrch" data-ew-action="search-type" data-search-type="OR">@Html.Raw(Language.Phrase("QuickSearchAny"))</button>
            </div>
        </div>
    </div>
    <div class="col-sm-auto mb-3">
        <button class="btn btn-primary" name="btn-submit" id="btn-submit" type="submit">@Html.Raw(Language.Phrase("SearchBtn"))</button>
    </div>
</div>
</div><!-- /.ew-extended-search -->
} @* Show search form (end) *@
}
</div><!-- /.ew-search-panel -->
</form>
}
@hijriTableList.ShowPageHeader()
@hijriTableList.ShowMessages()
<main class="list @(hijriTableList.TotalRecords == 0 && !hijriTableList.IsAdd ? " ew-no-record" : "")">
<div id="ew-list">
@if (hijriTableList.TotalRecords > 0 || !Empty(hijriTableList.CurrentAction)) {
<div class="ew-multi-column-grid p-0 overlay-wrapper">
@{ hijriTableList.LayoutOptions.Render("body"); }
@await Html.PartialAsync("HijriTable" + TitleCaseInvariant(hijriTableList.MultiColumnLayout))
</div><!-- /.ew-multi-column-grid -->
} else { // Show other options
<div class="ew-list-other-options">
@hijriTableList.OtherOptions.ForEach(opt => opt.ButtonClass = "").RenderBody()
</div>
}
</div>
</main>
@hijriTableList.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
@if (!hijriTableList.IsExport()) {
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("Hijri_Table");
});
</script>
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
}
