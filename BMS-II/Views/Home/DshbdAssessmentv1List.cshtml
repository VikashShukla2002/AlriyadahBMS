@{
    // View
    View = this;

    // Check action result
    if (dshbdAssessmentv1List.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!dshbdAssessmentv1List.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
@if (!dshbdAssessmentv1List.IsExport()) {
<script>
var currentTable = @Html.Raw(ConvertToJson(dshbdAssessmentv1List.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { dshbdAssessmentv1: currentTable } });
var currentPageID = ew.PAGE_ID = "list";
var currentForm;
var @(dshbdAssessmentv1List.FormName);
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("@(dshbdAssessmentv1List.FormName)")
        .setPageId("list")
        .setSubmitWithFetch(@(dshbdAssessmentv1List.UseAjaxActions ? "true" : "false"))
        .setFormKeyCountName("@(dshbdAssessmentv1List.FormKeyCountName)")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
ew.PREVIEW_SELECTOR = ".ew-preview-btn";
ew.PREVIEW_MODAL_CLASS = "modal modal-fullscreen-sm-down";
ew.PREVIEW_ROW = true;
ew.PREVIEW_SINGLE_ROW = false;
ew.PREVIEW || ew.ready("head", ew.PATH_BASE + "js/preview.min.js?v=20.5.0", "preview");
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
}
@if (!dshbdAssessmentv1List.IsExport()) {
<div class="btn-toolbar ew-toolbar">
@if (dshbdAssessmentv1List.TotalRecords > 0 && dshbdAssessmentv1List.ExportOptions.Visible) {
    dshbdAssessmentv1List.ExportOptions.Render("body");
}
@if (dshbdAssessmentv1List.ImportOptions.Visible) {
    dshbdAssessmentv1List.ImportOptions.Render("body");
}
@if (dshbdAssessmentv1List.SearchOptions.Visible) {
    dshbdAssessmentv1List.SearchOptions.Render("body");
}
@if (dshbdAssessmentv1List.FilterOptions.Visible) {
    dshbdAssessmentv1List.FilterOptions.Render("body");
}
</div>
}
@if (!dshbdAssessmentv1List.IsModal) {
<form name="fdshbdAssessmentv1srch" id="fdshbdAssessmentv1srch" class="ew-form ew-ext-search-form" action="@AppPath(CurrentPageName())" novalidate autocomplete="off">
<div id="fdshbdAssessmentv1srch_search_panel" class="mb-2 mb-sm-0 @(dshbdAssessmentv1List.SearchPanelClass)"><!-- .ew-search-panel -->
<script>
var currentTable = @Html.Raw(ConvertToJson(dshbdAssessmentv1List.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { dshbdAssessmentv1: currentTable } });
var currentForm;
var fdshbdAssessmentv1srch, currentSearchForm, currentAdvancedSearchForm;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery,
        fields = currentTable.fields;

    // Form object for search
    let form = new ew.FormBuilder()
        .setId("fdshbdAssessmentv1srch")
        .setPageId("list")
@if (dshbdAssessmentv1List.UseAjaxActions) {
        <text>.setSubmitWithFetch(true)</text>
}

        // Dynamic selection lists
        .setLists({
        })

        // Filters
        .setFilterList(@Html.Raw(await dshbdAssessmentv1List.GetFilterList()))
        .build();
    window[form.id] = form;
    currentSearchForm = form;
    loadjs.done(form.id);
});
</script>
<input type="hidden" name="cmd" value="search">
@if (Security.CanSearch) {
if (!dshbdAssessmentv1List.IsExport() && Empty(dshbdAssessmentv1List.CurrentAction) && dshbdAssessmentv1List.HasSearchFields()) { @* Show search form (begin) *@
<div class="ew-extended-search container-fluid ps-2">
<div class="row mb-0">
    <div class="col-sm-auto px-0 pe-sm-2">
        <div class="ew-basic-search input-group">
            <input type="search" name="@(Config.TableBasicSearch)" id="@(Config.TableBasicSearch)" class="form-control ew-basic-search-keyword" value="@Html.Raw(HtmlEncode(dshbdAssessmentv1List.BasicSearch.SessionKeyword))" placeholder="@(HtmlEncode(Language.Phrase("Search")))" aria-label="@(HtmlEncode(Language.Phrase("Search")))">
            <input type="hidden" name="@(Config.TableBasicSearchType)" id="@(Config.TableBasicSearchType)" class="ew-basic-search-type" value="@(HtmlEncode(dshbdAssessmentv1List.BasicSearch.SessionType))">
            <button type="button" data-bs-toggle="dropdown" class="btn btn-outline-secondary dropdown-toggle dropdown-toggle-split" aria-haspopup="true" aria-expanded="false">
                <span id="searchtype">@Html.Raw(dshbdAssessmentv1List.BasicSearch.TypeNameShort)</span>
            </button>
            <div class="dropdown-menu dropdown-menu-end">
                <button type="button" class="dropdown-item@(dshbdAssessmentv1List.BasicSearch.SessionType == "" ? " active" : "")" form="fdshbdAssessmentv1srch" data-ew-action="search-type">@Html.Raw(Language.Phrase("QuickSearchAuto"))</button>
                <button type="button" class="dropdown-item@(dshbdAssessmentv1List.BasicSearch.SessionType == "=" ? " active" : "")" form="fdshbdAssessmentv1srch" data-ew-action="search-type" data-search-type="=">@Html.Raw(Language.Phrase("QuickSearchExact"))</button>
                <button type="button" class="dropdown-item@(dshbdAssessmentv1List.BasicSearch.SessionType == "AND" ? " active" : "")" form="fdshbdAssessmentv1srch" data-ew-action="search-type" data-search-type="AND">@Html.Raw(Language.Phrase("QuickSearchAll"))</button>
                <button type="button" class="dropdown-item@(dshbdAssessmentv1List.BasicSearch.SessionType == "OR" ? " active" : "")" form="fdshbdAssessmentv1srch" data-ew-action="search-type" data-search-type="OR">@Html.Raw(Language.Phrase("QuickSearchAny"))</button>
            </div>
        </div>
    </div>
    <div class="col-sm-auto mb-3">
        <button class="btn btn-primary" name="btn-submit" id="btn-submit" type="submit">@Html.Raw(Language.Phrase("SearchBtn"))</button>
    </div>
</div>
</div><!-- /.ew-extended-search -->
} @* Show search form (end) *@
}
</div><!-- /.ew-search-panel -->
</form>
}
@dshbdAssessmentv1List.ShowPageHeader()
@dshbdAssessmentv1List.ShowMessages()
<main class="list @(dshbdAssessmentv1List.TotalRecords == 0 && !dshbdAssessmentv1List.IsAdd ? " ew-no-record" : "")">
<div id="ew-list">
@if (dshbdAssessmentv1List.TotalRecords > 0 || !Empty(dshbdAssessmentv1List.CurrentAction)) {
<div class="ew-multi-column-grid p-0 overlay-wrapper">
@{ dshbdAssessmentv1List.LayoutOptions.Render("body"); }
@await Html.PartialAsync("DshbdAssessmentv1" + TitleCaseInvariant(dshbdAssessmentv1List.MultiColumnLayout))
</div><!-- /.ew-multi-column-grid -->
} else { // Show other options
<div class="ew-list-other-options">
@dshbdAssessmentv1List.OtherOptions.ForEach(opt => opt.ButtonClass = "").RenderBody()
</div>
}
</div>
</main>
@dshbdAssessmentv1List.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
@if (!dshbdAssessmentv1List.IsExport()) {
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("dshbdAssessmentv1");
});
</script>
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
}
