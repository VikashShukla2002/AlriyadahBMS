@if (qryAppointmentTypeList.ModalGridAdd && qryAppointmentTypeList.IsGridAdd || qryAppointmentTypeList.ModalGridEdit && qryAppointmentTypeList.IsGridEdit || qryAppointmentTypeList.ModalMultiEdit && qryAppointmentTypeList.IsMultiEdit) {
<script>
var currentTable = @Html.Raw(ConvertToJson(qryAppointmentTypeList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { qryAppointmentType: currentTable } });
var currentPageID = ew.PAGE_ID = "list";
var currentForm;
var @(qryAppointmentTypeList.FormName);
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("@(qryAppointmentTypeList.FormName)")
        .setPageId("list")
        .setSubmitWithFetch(@(qryAppointmentTypeList.UseAjaxActions ? "true" : "false"))
        .setFormKeyCountName("@(qryAppointmentTypeList.FormKeyCountName)")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
ew.PREVIEW_SELECTOR = ".ew-preview-btn";
ew.PREVIEW_MODAL_CLASS = "modal modal-fullscreen-sm-down";
ew.PREVIEW_ROW = true;
ew.PREVIEW_SINGLE_ROW = false;
ew.PREVIEW || ew.ready("head", ew.PATH_BASE + "js/preview.min.js?v=20.5.0", "preview");
</script>
}
<div class="card ew-card ew-grid@(qryAppointmentTypeList.IsAddOrEdit ? " ew-grid-add-edit" : "") @(qryAppointmentTypeList.TableGridClass)">
@if (!qryAppointmentTypeList.IsExport()) {
<div class="card-header ew-grid-upper-panel">
@if (!qryAppointmentTypeList.IsGridAdd && !(qryAppointmentTypeList.IsGridEdit && qryAppointmentTypeList.ModalGridEdit) && !qryAppointmentTypeList.IsMultiEdit) {
@Html.Raw(qryAppointmentTypeList.Pager.Render())
}
<div class="ew-list-other-options">
@qryAppointmentTypeList.OtherOptions.RenderBody()
</div>
</div>
}
<form name="@(qryAppointmentTypeList.FormName)" id="@(qryAppointmentTypeList.FormName)" class="ew-form ew-list-form ew-multi-column-form" action="@AppPath(qryAppointmentTypeList.PageName)" method="post" novalidate autocomplete="off">
@if (CurrentPage.CheckToken) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
@if (qryAppointmentTypeList.IsModal) {
<input type="hidden" name="modal" value="1">
}
<div id="gmp_qryAppointmentType" class="card-body ew-grid-middle-panel @(qryAppointmentTypeList.TableContainerClass)" style="@(qryAppointmentTypeList.TableContainerStyle)">
@if (qryAppointmentTypeList.TotalRecords > 0 || qryAppointmentTypeList.IsGridEdit || qryAppointmentTypeList.IsMultiEdit) {
<table id="tbl_qryAppointmentTypelist" class="@(qryAppointmentTypeList.TableClass)"><!-- .ew-table -->
<thead>
    <tr class="ew-table-header">
@{
    // Header row
    qryAppointmentTypeList.RowType = RowType.Header;

    // Render list options
    await qryAppointmentTypeList.RenderListOptions();
}
@* Render list options (header, left) *@
@qryAppointmentTypeList.ListOptions.RenderHeader("left")
@if (qryAppointmentTypeList.int_PackageID.Visible) { // qryAppointmentTypeList.int_PackageID.Visible (begin)
        <th data-name="int_PackageID" class="@(qryAppointmentTypeList.int_PackageID.HeaderCellClass)"><div id="elh_qryAppointmentType_int_PackageID" class="qryAppointmentType_int_PackageID">@Html.Raw(qryAppointmentTypeList.RenderFieldHeader(qryAppointmentTypeList.int_PackageID))</div></th>
} @* qryAppointmentTypeList.int_PackageID.Visible (end) *@
@if (qryAppointmentTypeList.Package_Name.Visible) { // qryAppointmentTypeList.Package_Name.Visible (begin)
        <th data-name="Package_Name" class="@(qryAppointmentTypeList.Package_Name.HeaderCellClass)"><div id="elh_qryAppointmentType_Package_Name" class="qryAppointmentType_Package_Name">@Html.Raw(qryAppointmentTypeList.RenderFieldHeader(qryAppointmentTypeList.Package_Name))</div></th>
} @* qryAppointmentTypeList.Package_Name.Visible (end) *@
@if (qryAppointmentTypeList.int_AppType.Visible) { // qryAppointmentTypeList.int_AppType.Visible (begin)
        <th data-name="int_AppType" class="@(qryAppointmentTypeList.int_AppType.HeaderCellClass)"><div id="elh_qryAppointmentType_int_AppType" class="qryAppointmentType_int_AppType">@Html.Raw(qryAppointmentTypeList.RenderFieldHeader(qryAppointmentTypeList.int_AppType))</div></th>
} @* qryAppointmentTypeList.int_AppType.Visible (end) *@
@if (qryAppointmentTypeList.Service_Description.Visible) { // qryAppointmentTypeList.Service_Description.Visible (begin)
        <th data-name="Service_Description" class="@(qryAppointmentTypeList.Service_Description.HeaderCellClass)"><div id="elh_qryAppointmentType_Service_Description" class="qryAppointmentType_Service_Description">@Html.Raw(qryAppointmentTypeList.RenderFieldHeader(qryAppointmentTypeList.Service_Description))</div></th>
} @* qryAppointmentTypeList.Service_Description.Visible (end) *@
@if (qryAppointmentTypeList.Price.Visible) { // qryAppointmentTypeList.Price.Visible (begin)
        <th data-name="Price" class="@(qryAppointmentTypeList.Price.HeaderCellClass)"><div id="elh_qryAppointmentType_Price" class="qryAppointmentType_Price">@Html.Raw(qryAppointmentTypeList.RenderFieldHeader(qryAppointmentTypeList.Price))</div></th>
} @* qryAppointmentTypeList.Price.Visible (end) *@
@* Render list options (header, right) *@
@qryAppointmentTypeList.ListOptions.RenderHeader("right")
    </tr>
</thead>
<tbody data-page="@(qryAppointmentTypeList.PageNumber)">
@{
    await qryAppointmentTypeList.SetupGrid();
    bool eof = qryAppointmentTypeList.Recordset == null; // DN
}
@while (qryAppointmentTypeList.RecordCount < qryAppointmentTypeList.StopRecord || SameString(qryAppointmentTypeList.RowIndex, "$rowindex$")) { // Begin While
    if (!eof && !SameString(qryAppointmentTypeList.RowIndex, "$rowindex$") &&
        (!qryAppointmentTypeList.IsGridAdd || qryAppointmentTypeList.CurrentMode == "copy") &&
        (!((qryAppointmentTypeList.IsCopy || qryAppointmentTypeList.IsAdd) && qryAppointmentTypeList.RowIndex is int rowIndex && rowIndex == 0))) // DN
        eof = !await qryAppointmentTypeList.Recordset.ReadAsync();
    qryAppointmentTypeList.RecordCount++;
    if (qryAppointmentTypeList.RecordCount >= qryAppointmentTypeList.StartRecord) {
        await qryAppointmentTypeList.SetupRow();
    <tr @Html.Raw(qryAppointmentTypeList.RowAttributes)>
    @* Render list options (body, left) *@
    @qryAppointmentTypeList.ListOptions.RenderBody("left", qryAppointmentTypeList.RowCount)
    @if (qryAppointmentTypeList.int_PackageID.Visible) { // qryAppointmentTypeList.int_PackageID.Visible (begin)
        <td data-name="int_PackageID" @Html.Raw(qryAppointmentTypeList.int_PackageID.CellAttributes)>
<span id="el@(qryAppointmentTypeList.RowIndex == "$rowindex$" ? "$rowindex$" : qryAppointmentTypeList.RowCount)_qryAppointmentType_int_PackageID" class="el_qryAppointmentType_int_PackageID">
<span @Html.Raw(qryAppointmentTypeList.int_PackageID.ViewAttributes)>
@Html.Raw(qryAppointmentTypeList.int_PackageID.GetViewValue())</span>
</span>
</td>
    } @* qryAppointmentTypeList.int_PackageID.Visible (end) *@
    @if (qryAppointmentTypeList.Package_Name.Visible) { // qryAppointmentTypeList.Package_Name.Visible (begin)
        <td data-name="Package_Name" @Html.Raw(qryAppointmentTypeList.Package_Name.CellAttributes)>
<span id="el@(qryAppointmentTypeList.RowIndex == "$rowindex$" ? "$rowindex$" : qryAppointmentTypeList.RowCount)_qryAppointmentType_Package_Name" class="el_qryAppointmentType_Package_Name">
<span @Html.Raw(qryAppointmentTypeList.Package_Name.ViewAttributes)>
@Html.Raw(qryAppointmentTypeList.Package_Name.GetViewValue())</span>
</span>
</td>
    } @* qryAppointmentTypeList.Package_Name.Visible (end) *@
    @if (qryAppointmentTypeList.int_AppType.Visible) { // qryAppointmentTypeList.int_AppType.Visible (begin)
        <td data-name="int_AppType" @Html.Raw(qryAppointmentTypeList.int_AppType.CellAttributes)>
<span id="el@(qryAppointmentTypeList.RowIndex == "$rowindex$" ? "$rowindex$" : qryAppointmentTypeList.RowCount)_qryAppointmentType_int_AppType" class="el_qryAppointmentType_int_AppType">
<span @Html.Raw(qryAppointmentTypeList.int_AppType.ViewAttributes)>
@Html.Raw(qryAppointmentTypeList.int_AppType.GetViewValue())</span>
</span>
</td>
    } @* qryAppointmentTypeList.int_AppType.Visible (end) *@
    @if (qryAppointmentTypeList.Service_Description.Visible) { // qryAppointmentTypeList.Service_Description.Visible (begin)
        <td data-name="Service_Description" @Html.Raw(qryAppointmentTypeList.Service_Description.CellAttributes)>
<span id="el@(qryAppointmentTypeList.RowIndex == "$rowindex$" ? "$rowindex$" : qryAppointmentTypeList.RowCount)_qryAppointmentType_Service_Description" class="el_qryAppointmentType_Service_Description">
<span @Html.Raw(qryAppointmentTypeList.Service_Description.ViewAttributes)>
@Html.Raw(qryAppointmentTypeList.Service_Description.GetViewValue())</span>
</span>
</td>
    } @* qryAppointmentTypeList.Service_Description.Visible (end) *@
    @if (qryAppointmentTypeList.Price.Visible) { // qryAppointmentTypeList.Price.Visible (begin)
        <td data-name="Price" @Html.Raw(qryAppointmentTypeList.Price.CellAttributes)>
<span id="el@(qryAppointmentTypeList.RowIndex == "$rowindex$" ? "$rowindex$" : qryAppointmentTypeList.RowCount)_qryAppointmentType_Price" class="el_qryAppointmentType_Price">
<span @Html.Raw(qryAppointmentTypeList.Price.ViewAttributes)>
@Html.Raw(qryAppointmentTypeList.Price.GetViewValue())</span>
</span>
</td>
    } @* qryAppointmentTypeList.Price.Visible (end) *@
    @* Render list options (body, right) *@
    @qryAppointmentTypeList.ListOptions.RenderBody("right", qryAppointmentTypeList.RowCount)
    </tr>
    } @* End if *@

    // Reset for template row
    if (SameString(qryAppointmentTypeList.RowIndex, "$rowindex$"))
        qryAppointmentTypeList.RowIndex = 0;

    // Reset inline add/copy row
    if ((qryAppointmentTypeList.IsCopy || qryAppointmentTypeList.IsAdd) && qryAppointmentTypeList.RowIndex is int rowIndex2 && rowIndex2 == 0)
        qryAppointmentTypeList.RowIndex = 1;
} @* End While *@
</tbody>
</table><!-- /.ew-table -->
} @* End If TotalRecords > 0 *@
</div><!-- /.ew-grid-middle-panel -->
@if (Empty(qryAppointmentTypeList.CurrentAction) && !qryAppointmentTypeList.UseAjaxActions) {
<input type="hidden" name="action" id="action" value="">
}
</form><!-- /.ew-list-form -->
@{ qryAppointmentTypeList.CloseRecordset(); }
</div><!-- /.ew-grid -->
