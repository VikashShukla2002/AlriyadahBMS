@{
    // View
    View = this;

    // Check action result
    if (qryAppointmentTypeList.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!qryAppointmentTypeList.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
@if (!qryAppointmentTypeList.IsExport()) {
<script>
var currentTable = @Html.Raw(ConvertToJson(qryAppointmentTypeList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { qryAppointmentType: currentTable } });
var currentPageID = ew.PAGE_ID = "list";
var currentForm;
var @(qryAppointmentTypeList.FormName);
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("@(qryAppointmentTypeList.FormName)")
        .setPageId("list")
        .setSubmitWithFetch(@(qryAppointmentTypeList.UseAjaxActions ? "true" : "false"))
        .setFormKeyCountName("@(qryAppointmentTypeList.FormKeyCountName)")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
ew.PREVIEW_SELECTOR = ".ew-preview-btn";
ew.PREVIEW_MODAL_CLASS = "modal modal-fullscreen-sm-down";
ew.PREVIEW_ROW = true;
ew.PREVIEW_SINGLE_ROW = false;
ew.PREVIEW || ew.ready("head", ew.PATH_BASE + "js/preview.min.js?v=20.5.0", "preview");
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
}
@if (!qryAppointmentTypeList.IsExport()) {
<div class="btn-toolbar ew-toolbar">
@if (qryAppointmentTypeList.TotalRecords > 0 && qryAppointmentTypeList.ExportOptions.Visible) {
    qryAppointmentTypeList.ExportOptions.Render("body");
}
@if (qryAppointmentTypeList.ImportOptions.Visible) {
    qryAppointmentTypeList.ImportOptions.Render("body");
}
@if (qryAppointmentTypeList.SearchOptions.Visible) {
    qryAppointmentTypeList.SearchOptions.Render("body");
}
@if (qryAppointmentTypeList.FilterOptions.Visible) {
    qryAppointmentTypeList.FilterOptions.Render("body");
}
</div>
}
@if (!qryAppointmentTypeList.IsModal) {
<form name="fqryAppointmentTypesrch" id="fqryAppointmentTypesrch" class="ew-form ew-ext-search-form" action="@AppPath(CurrentPageName())" novalidate autocomplete="off">
<div id="fqryAppointmentTypesrch_search_panel" class="mb-2 mb-sm-0 @(qryAppointmentTypeList.SearchPanelClass)"><!-- .ew-search-panel -->
<script>
var currentTable = @Html.Raw(ConvertToJson(qryAppointmentTypeList.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { qryAppointmentType: currentTable } });
var currentForm;
var fqryAppointmentTypesrch, currentSearchForm, currentAdvancedSearchForm;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery,
        fields = currentTable.fields;

    // Form object for search
    let form = new ew.FormBuilder()
        .setId("fqryAppointmentTypesrch")
        .setPageId("list")
@if (qryAppointmentTypeList.UseAjaxActions) {
        <text>.setSubmitWithFetch(true)</text>
}

        // Dynamic selection lists
        .setLists({
        })

        // Filters
        .setFilterList(@Html.Raw(await qryAppointmentTypeList.GetFilterList()))
        .build();
    window[form.id] = form;
    currentSearchForm = form;
    loadjs.done(form.id);
});
</script>
<input type="hidden" name="cmd" value="search">
@if (Security.CanSearch) {
if (!qryAppointmentTypeList.IsExport() && Empty(qryAppointmentTypeList.CurrentAction) && qryAppointmentTypeList.HasSearchFields()) { @* Show search form (begin) *@
<div class="ew-extended-search container-fluid ps-2">
<div class="row mb-0">
    <div class="col-sm-auto px-0 pe-sm-2">
        <div class="ew-basic-search input-group">
            <input type="search" name="@(Config.TableBasicSearch)" id="@(Config.TableBasicSearch)" class="form-control ew-basic-search-keyword" value="@Html.Raw(HtmlEncode(qryAppointmentTypeList.BasicSearch.SessionKeyword))" placeholder="@(HtmlEncode(Language.Phrase("Search")))" aria-label="@(HtmlEncode(Language.Phrase("Search")))">
            <input type="hidden" name="@(Config.TableBasicSearchType)" id="@(Config.TableBasicSearchType)" class="ew-basic-search-type" value="@(HtmlEncode(qryAppointmentTypeList.BasicSearch.SessionType))">
            <button type="button" data-bs-toggle="dropdown" class="btn btn-outline-secondary dropdown-toggle dropdown-toggle-split" aria-haspopup="true" aria-expanded="false">
                <span id="searchtype">@Html.Raw(qryAppointmentTypeList.BasicSearch.TypeNameShort)</span>
            </button>
            <div class="dropdown-menu dropdown-menu-end">
                <button type="button" class="dropdown-item@(qryAppointmentTypeList.BasicSearch.SessionType == "" ? " active" : "")" form="fqryAppointmentTypesrch" data-ew-action="search-type">@Html.Raw(Language.Phrase("QuickSearchAuto"))</button>
                <button type="button" class="dropdown-item@(qryAppointmentTypeList.BasicSearch.SessionType == "=" ? " active" : "")" form="fqryAppointmentTypesrch" data-ew-action="search-type" data-search-type="=">@Html.Raw(Language.Phrase("QuickSearchExact"))</button>
                <button type="button" class="dropdown-item@(qryAppointmentTypeList.BasicSearch.SessionType == "AND" ? " active" : "")" form="fqryAppointmentTypesrch" data-ew-action="search-type" data-search-type="AND">@Html.Raw(Language.Phrase("QuickSearchAll"))</button>
                <button type="button" class="dropdown-item@(qryAppointmentTypeList.BasicSearch.SessionType == "OR" ? " active" : "")" form="fqryAppointmentTypesrch" data-ew-action="search-type" data-search-type="OR">@Html.Raw(Language.Phrase("QuickSearchAny"))</button>
            </div>
        </div>
    </div>
    <div class="col-sm-auto mb-3">
        <button class="btn btn-primary" name="btn-submit" id="btn-submit" type="submit">@Html.Raw(Language.Phrase("SearchBtn"))</button>
    </div>
</div>
</div><!-- /.ew-extended-search -->
} @* Show search form (end) *@
}
</div><!-- /.ew-search-panel -->
</form>
}
@qryAppointmentTypeList.ShowPageHeader()
@qryAppointmentTypeList.ShowMessages()
<main class="list @(qryAppointmentTypeList.TotalRecords == 0 && !qryAppointmentTypeList.IsAdd ? " ew-no-record" : "")">
<div id="ew-list">
@if (qryAppointmentTypeList.TotalRecords > 0 || !Empty(qryAppointmentTypeList.CurrentAction)) {
<div class="ew-multi-column-grid p-0 overlay-wrapper">
@{ qryAppointmentTypeList.LayoutOptions.Render("body"); }
@await Html.PartialAsync("QryAppointmentType" + TitleCaseInvariant(qryAppointmentTypeList.MultiColumnLayout))
</div><!-- /.ew-multi-column-grid -->
} else { // Show other options
<div class="ew-list-other-options">
@qryAppointmentTypeList.OtherOptions.ForEach(opt => opt.ButtonClass = "").RenderBody()
</div>
}
</div>
</main>
@qryAppointmentTypeList.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
@if (!qryAppointmentTypeList.IsExport()) {
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("qryAppointmentType");
});
</script>
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
}
